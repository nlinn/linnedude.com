<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta charset="utf-8">
    <title><%= @post.present? ? truncate("#{@post.title} (linnedude.com)", length: 80) : "linnedude.com" %></title>

    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
      <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
      <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
      <link rel="manifest" href="/site.webmanifest">
    <%= render "layouts/twitter_card" %>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <% stylesheet_pack_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag "application", 'data-turbolinks-track': 'reload' %>

    <% if content_for?(:feed_url) %>
      <%= auto_discovery_link_tag :rss, yield(:feed_url) %>
    <% end %>

    <link rel="stylesheet"
      href="//cdnjs.cloudflare.com/ajax/libs/highlight.js/10.5.0/styles/default.min.css">

  </head>

  <body class="admin">
      <div class="admin-container">
        <div class="admin-panel left">
          <div class="basic" style="text-align: center; margin: 2rem auto 3rem auto;">
            <img src="/apple-touch-icon.png" style="display: inline-block; width: 90px; height: 90px;">
          </div>

          <menu>
            <li class="admin-nav"><%= link_to "New post", new_post_path %></li>
            <li class="admin-nav"><%= link_to "All Posts", posts_path %></li>
            <li class="admin-nav"><%= link_to "All Photos", photos_path %></li>
            <li class="admin-nav"><%= link_to "All Labels", labels_path %></li>
          </menu>

          <div class="basic" style="margin: 1rem;">
            <%= current_user.email %>
            <div>Locale: <%= I18n.locale %></div>
            <div><%= link_to "Sign out", sign_out_path, method: :delete %></div>
          </div>

        </div>
        <%= yield %>
      </div>

  </body>
</html>











